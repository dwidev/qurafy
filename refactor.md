Refactor this React/Next.js component into smaller components.

Requirements:
- Split UI sections into separate components
- Extract repeated JSX into reusable components
- Use meaningful component names
- Keep each component focused on one responsibility
- Move reusable elements into `components/ui`

Return:
1. Refactored main component
2. New smaller components
3. Suggested folder structure